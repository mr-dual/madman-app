# Gradle build outputs and caches
.gradle/
build/
app/build/

# Local configuration files (contains your local SDK paths)
local.properties

# Android Studio / IntelliJ files (just in case they sneak in)
.idea/
*.iml
.modules/

# CMake / Native compilation build junk
compile_commands.json
.cxx/
*.so
*.a
*.o
.cache/

# OS generated files
.DS_Store
Thumbs.db

# JNI Libraries
*/src/*/jniLibs
